<h1 class = "tweet_more">つぶやきを詳しく</h1>

</br>

<p><%= @blog.content %></p>

</br>

<% unless @blog.user == current_user %>
  <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する',
    favorite_path(blog_id: @blog.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする',
    favorites_path(blog_id: @blog.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %>

</br>

<td><%= link_to '一覧に戻る', blogs_path %></td>
